import 'package:flutter/cupertino.dart';

class NewFeedPage extends StatelessWidget {
  const NewFeedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('NewFeedPage'),
    );
  }
}
